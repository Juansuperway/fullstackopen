```mermaid
sequenceDiagram
    participant browser
    participant server

    browser->>server: POST new note data
    activate server
    server-->>browser: confirmation / updated note list
    deactivate server

    Note right of browser: Browser updates the note list without reloading the page
    
```