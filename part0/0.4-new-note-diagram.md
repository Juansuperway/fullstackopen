```mermaid
sequenceDiagram
    participant browser
    participant server

    browser->>server: POST new note data
    activate server
    server->>browser: confirmation / update note list
    deactivate server

    note right of browser: Browser executres callback and update the note list
```